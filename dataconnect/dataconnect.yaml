specVersion: "v1beta"
serviceId: "food finder"
location: "europe-central2"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "food finder-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
